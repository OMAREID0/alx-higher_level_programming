#!/uar/bin/python3
"""Check instance of the specified class"""

def is_same_class(obj, a_class):
    """return true if the same class"""
    if(type(obj) == a_class):
        return (True)
    else:
        return (False)